export function validateRequiredField(value: string): boolean {
  // if(!value){
  //   return false
  // } else {
  //   return true
  // }

  // return value ? true : false;
  return !!value;
}
